`queries` 배열의 각 요소를 순회하며, 주어진 조건을 만족하는 `arr[i]`에 1을 더해주세요.

## 코드

```js
function solution(arr, queries) {
  var answer = [...arr];
  queries.forEach(([s, e, k]) => {
    for (let i = s; i <= e; i++) {
      if (i % k === 0) {
        answer[i]++;
      }
    }
  });

  return answer;
}
```
