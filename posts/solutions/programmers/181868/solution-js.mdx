## 이론

### 정규표현식

`match(){:js}` 메서드를 사용해서 조건에 맞는 문자열을 찾을 수 있어요.
여기에서는 `/\w+/g{:js}` 정규표현식을 사용했어요.
이 표현식은 문자열에서 단어를 찾아주는 표현식이예요.

## 코드

```js
function solution(my_string) {
  return my_string.match(/\w+/g);
}
```
