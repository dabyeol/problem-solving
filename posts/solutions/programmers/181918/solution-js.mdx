## 코드

```js
function solution(arr) {
  var stk = [];
  for (let i = 0; i < arr.length; ) {
    if (stk.length === 0) {
      stk.push(arr[i++]);
    } else if (stk.at(-1) < arr[i]) {
      stk.push(arr[i++]);
    } else {
      stk.pop();
    }
  }

  return stk;
}
```
