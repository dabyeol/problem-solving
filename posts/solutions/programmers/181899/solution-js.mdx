길이가 `start_num - end_num + 1{:js}`인 배열을 만들고, `map(){:js}`을 이용해 `start_num`부터 `end_num`까지의 숫자로 채워진 배열을 만들어요.

## 코드

```js
function solution(start_num, end_num) {
  return [...Array(start_num - end_num + 1)].map((_, i) => start_num - i);
}
```
