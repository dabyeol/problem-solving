import Reduce from '@/posts/theories/js/array/reduce.mdx';
import Slice from '@/posts/theories/js/array/slice.mdx';

## 이론

<Reduce />
<Slice />

## 코드

```js
function solution(arr, query) {
  return query.reduce((acc, cur, idx) => {
    if (idx % 2) {
      return acc.slice(cur);
    } else {
      return acc.slice(0, cur + 1);
    }
  }, arr);
}
```
