import Map from '@/posts/theories/js/map.mdx';

옷의 종류별로 개수를 센 다음 `((개수 + 1)의 곱) - 1`을 구하면 돼요.
개수에 1을 더하는 이유는 해당 종류의 옷을 입지 않는 경우도 포함하기 위해서예요.
마지막에 1을 빼는 이유는 모든 옷을 입지 않는 경우는 제외하기 위해서예요.

## 이론

<Map />
