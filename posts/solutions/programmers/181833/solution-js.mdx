## 이론

`map(){:js}` 메서드를 중첩해서 사용하면 2차원 배열을 만들 수 있어요.
`i`와 `j`가 같을 때만 `1`을 넣어요.

## 코드

```js
function solution(n) {
  return [...Array(n)].map((_, i) =>
    [...Array(n)].map((_, j) => Number(i === j))
  );
}
```
