import Reduce from '@/posts/theories/js/array/reduce.mdx';
import Splice from '@/posts/theories/js/array/splice.mdx';
import Slice from '@/posts/theories/js/array/slice.mdx';

`map(){:js}` 함수를 이용해서 먼저 number를 숫자 배열로 바꿔요. 그리고 `reduce(){:js}` 함수를 이용해서 배열의 합을 구하고 9로 나눈 나머지를 구해요.

`reduce(){:js}` 함수 안에서 `splice(){:js}` 함수를 이용해서 배열 내용을 변경해요. `reverse(){:js}` 함수를 이용해서 배열을 뒤집어요.

## 이론

<Reduce />
<Splice />
<Slice />
