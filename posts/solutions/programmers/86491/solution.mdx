풀기 전에 명함을 회전시켜야 해요. 명함에서 긴 변이 가로 길이가 되도록 회전시킬 수 있어요.
이렇게 회전시킨 명함을 가지고 지갑의 크기를 계산하려면, 가로 길이 중 최댓값이 지갑의 가로 길이, 세로 길이 중 최댓값이 지갑의 세로 길이가 돼요.

각각의 명함 `[w, h]` 중 큰 값을 `a`, 작은 값을 `b`라고 하면, `a`들 중 최댓값이 지갑의 가로 길이, `b`들 중 최댓값이 지갑의 세로 길이가 돼요.
