import StartsWith from '@/posts/theories/js/string/starts-with.mdx';

## 이론

<StartsWith />

## 코드

```js
function solution(my_string, is_prefix) {
  return Number(my_string.startsWith(is_prefix));
}
```
