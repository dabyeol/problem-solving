[**A+B (1000)**](../1000/js)를 먼저 풀어보세요!

## 이론

### 입력받기

한 줄에 하나씩 입력받는 방법에 대해 알아봐요.

```js {2}
const fs = require('fs');
const [x, y] = fs.readFileSync(0).toString().split('\n').map(Number);
```

`split(){:js}` 함수를 사용하여 줄바꿈 문자를 기준으로 문자열을 나누어 배열로 만들면 돼요.
