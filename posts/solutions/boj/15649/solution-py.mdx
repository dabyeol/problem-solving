> itertools 모듈의 permutations 함수를 사용하여 순열을 구할 수도 있어요.
>
> ```python
> from itertools import permutations
>
>
> n, m = map(int, input().split())
>
> for a in permutations(range(1, n + 1), m):
>     print(*a)
> ```
