> itertools 모듈의 combinations 함수를 사용하여 조합을 구할 수도 있어요.
>
> ```python
> from itertools import combinations
>
>
> n, m = map(int, input().split())
>
> for a in combinations(range(1, n + 1), m):
>     print(*a)
> ```
